# wissivity

Educational Activity game

## Lokaler Start (mit globalem Datensatz-Speicher)

```bash
node server.js
```

Danach im Browser öffnen: `http://localhost:4173`.

Der Endpunkt `/api/custom-datasets` speichert eigene CSV-Kartensätze global in `data/custom_datasets_db.json`.
